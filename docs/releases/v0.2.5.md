# v0.2.5

## 重点

- 增加 Host App 集成层：`RuntimeFacade`、`RuntimeProfile`、模型目录服务。
- 完善本地托管 `llama.cpp` 的 runtime 查找、模型发现和可用性字段。
- 将 `simple_chat` 迁移为 Host facade 使用示例。
- 增加 Host App 集成文档。

## 验证

- `nmake /NOLOGO`
- `qtllm_tests`
