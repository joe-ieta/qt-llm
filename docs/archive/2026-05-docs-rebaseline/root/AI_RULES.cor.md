# AI_RULES.md

这些规则是为在该存储库中工作的编码代理设计的。

## 务必做到

1. 优先使用 Qt 本地解决方案而非第三方抽象。
2. 保持网络请求的异步性。
3. 避免阻塞 UI 线程。
4. 保留 `QtLLMClient` $	o$ `ILLMProvider` 的抽象边界。
5. 使代码保持简洁、可读和可组合性。
6. 进行最小化、有针对性的更改，而非范围过大的推测性重构。
7. 优先使用在 Qt Creator 中能干净编译的源代码。
8. 为可重用的提供者逻辑，倾向于使用 OpenAI 兼容的传输结构。
9. 将提供商特定的逻辑放入提供者类中，而不是 UI 代码中。
10. 在引入新的架构概念时，添加文档说明。

## 避免

1. 添加了不充分合理的重度依赖。
2. 将 UI 渲染细节混入网络或提供者代码。
3. 在整个项目中硬编码一个提供者。
4. 引入同步等待模式。
5. 除非绝对必要，否则不使用复杂的模板元编程。
6. 大规模重构但没有更新所有上下文文件。

## 预期的实现风格

- 使用 Qt 信号/槽进行异步通知。
- 使用 `QNetworkAccessManager` 进行 HTTP 操作。
- 使用 `QJsonObject`、`QJsonArray` 和 `QJsonDocument` 处理 JSON 有效载荷。
- 在自然的情况下使用 `QString`、`QByteArray` 和 Qt 容器。
- 保持头文件简洁，实现文件直观。

## 当扩展功能时

- 如果出现新的子系统，请更新 `ARCHITECTURE.md`。
- 如果范围发生变化，请更新 `PROJECT_SPEC.md`。
- 对于重要的设计选择，请更新 `docs/DECISIONS.md`。